apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: qoves-db
  namespace: qoves-app
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: qoves-db
  podMetricsEndpoints:
    - port: metrics
