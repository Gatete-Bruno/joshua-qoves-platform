apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: qoves-api
  labels:
    app: qoves-api
spec:
  selector:
    matchLabels:
      app: qoves-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
