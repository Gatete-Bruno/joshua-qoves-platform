apiVersion: v1
kind: LimitRange
metadata:
  name: qoves-app-defaults
  namespace: qoves-app
spec:
  limits:
    - type: Container
      default:
        cpu: 200m
        memory: 128Mi
      defaultRequest:
        cpu: 50m
        memory: 64Mi
