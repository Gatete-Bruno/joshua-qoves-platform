# unrelated pod -> postgres:5432 (expect DENIED by default-deny)
All commands and output from this session will be recorded in container logs, including credentials and sensitive information passed through the command prompt.
If you don't see a command prompt, try pressing enter.
BLOCKED
pod "netpol-test" deleted from qoves-app namespace

# api pod -> https://api.github.com (expect allowed via FQDN policy)
200
# api pod -> https://example.com (expect blocked)
BLOCKED: URLError <urlopen error timed out>
